# econ-alert-bot 프로젝트 진행 기록

> 환율·금리 변동을 감지하고, 관련 뉴스와 연결해 설명을 생성한 뒤, 실시간으로 알려주는
> 금융 데이터 파이프라인 프로젝트. 금융IT 취업을 목표로 한 첫 개인 프로젝트로,
> 프로젝트/개발 경험이 전무한 상태에서 시작했다.

---

## 1. 프로젝트 개요

### 1.1 배경 및 동기
최근 세계 정세가 급변하면서 환율·금리 등 거시경제 지표가 빠르게 움직이고 있다.
이런 변화를 실시간으로 추적하고, 왜 그런 변화가 일어났는지 뉴스와 연결해
설명해주는 시스템을 만들고자 했다. 목적은 두 가지다.

1. 금융IT 분야 취업을 위한 포트폴리오 확보
2. 첫 프로젝트를 통해 "여러 기술을 엮어 하나의 동작하는 시스템을 만드는 법" 학습

### 1.2 최종 목표 아키텍처
```
[데이터 수집]  →  [데이터베이스 저장]  →  [분석 엔진]  →  [알림 / 대시보드]
 환율·금리 API      Oracle Cloud DB      변동 감지 +       텔레그램 봇 +
 + 뉴스/GDELT                            AI 설명 생성       웹 대시보드
```

### 1.3 사용 기술 스택
| 영역 | 기술 |
|---|---|
| 언어 | Python 3 |
| 데이터 수집 | `requests`, 한국은행 ECOS Open API |
| 환경 관리 | `venv`, `python-dotenv` |
| 데이터베이스 | Oracle Cloud Autonomous AI Database (Always Free Tier) |
| DB 드라이버 | `oracledb` (thin mode) |
| 버전 관리 | Git, GitHub |
| DB 클라이언트 | DBeaver (SQLD 학습 경험 활용) |

---

## 2. 단계별 진행 히스토리

### 2.1 단계 0 — 프로젝트 개발 환경 구축

**한 일**
- Python 설치 확인 (`python --version`)
- VS Code + Python 확장 설치
- GitHub에서 원격 저장소를 먼저 생성한 뒤 `git clone`으로 로컬에 가져오는 방식 채택
  (로컬에서 `git init` 후 `remote add`를 하는 것보다, README·`.gitignore`를 GitHub이
  자동 생성해주는 이 방식이 더 간단하다고 판단)
- 프로젝트 폴더 내부에 가상환경(`venv`) 생성 및 활성화

**새로 배운 개념**
- **가상환경(venv)이란**: 프로젝트별로 독립된 파이썬 라이브러리 공간을 만드는 것.
  전역 파이썬 환경을 오염시키지 않고, 프로젝트마다 다른 라이브러리 버전을 쓸 수 있게 해준다.
- **셸(shell)마다 다른 활성화 명령어**: 같은 Windows 환경이라도 사용하는 터미널
  (cmd, PowerShell, Git Bash)에 따라 가상환경 활성화 명령어가 다르다.
  - cmd/PowerShell: `venv\Scripts\activate`
  - Git Bash: `source venv/Scripts/activate` (Windows 폴더 구조 + Unix 계열 명령어가 섞인 형태)

**발생한 문제 및 해결**

| 문제 | 원인 | 해결 |
|---|---|---|
| `pip install` 후 VS Code가 "전역 환경에 설치된 것 같다"는 팝업 표시 | 가상환경이 활성화되지 않은 터미널에서 설치 명령을 실행함 | `which python` 명령으로 현재 파이썬 실행 경로에 `venv`가 포함되는지 확인하는 습관을 들임 |

---

### 2.2 단계 1 — 한국은행 ECOS Open API 연동

**핵심 개념: API 코드 체계 이해하기**

ECOS API는 3단계 계층 구조로 데이터를 식별한다.

```
통계표코드 (STAT_CODE)  →  항목코드 (ITEM_CODE)  →  실제 데이터 값
예: 731Y001                예: 0000001              예: 1380.3원
(어떤 표인가)               (표 안의 어떤 행인가)      (그 행의 실제 값)
```

이 구조를 몰랐다면 API 문서만 보고는 어떤 코드를 써야 할지 알 수 없었을 것이다.
그래서 다음과 같은 **탐색용 스크립트를 먼저 작성**하는 전략을 취했다.

**한 일 (스크립트 3종 세트)**

1. **`find_code.py`** — ECOS의 `StatisticTableList` API를 호출해 전체 통계표
   목록(839개)을 받아온 뒤, "대원화환율"이라는 키워드로 필터링해 원하는 표의
   코드(`731Y001`)를 찾아냄
2. **`find_item.py`** — 찾은 통계표코드로 `StatisticItemList` API를 호출해
   그 표 안의 세부 항목(원/달러, 원/엔, 원/위안 등) 목록을 받아온 뒤,
   "원/미국달러(매매기준율)"의 항목코드(`0000001`)를 찾아냄
3. **`main.py`** — 확보한 통계표코드(`731Y001`)와 항목코드(`0000001`)로
   `StatisticSearch` API를 호출해 실제 날짜별 환율 데이터를 수신

이 패턴("목록 조회 → 특정 → 실제 데이터 요청")은 처음 다뤄보는 API를 공략할 때
일반적으로 쓰이는 접근법이라는 것을 알게 됐다. 앞으로 뉴스 API 등을 연동할 때도
동일한 순서를 적용할 수 있다.

**새로 배운 개념**
- **API 응답 구조를 먼저 눈으로 확인하는 디버깅 습관**: `KeyError`가 발생했을 때,
  곧바로 코드를 고치기보다 `print(response_data)`로 실제 응답 전체를 출력해
  예상한 키 이름과 실제 키 이름이 다른지부터 확인하는 습관을 들였다.
  (예: `ITEM_CODE1`이라고 짐작했지만 실제로는 `ITEM_CODE`였음)
- **날짜 자동 계산**: 하드코딩된 날짜 대신 `datetime`, `timedelta`를 사용해
  "오늘 기준 최근 N일"을 매번 자동으로 계산하도록 구현. 스케줄러로 자동 실행할
  미래를 고려한 설계.
- **금융 데이터의 특성**: 환율은 외환시장이 열리는 평일에만 고시되므로, 주말·공휴일
  데이터는 API 응답 자체에 존재하지 않는다. 이는 클라이언트 쪽에서 별도로 필터링할
  필요 없이 서버가 이미 처리해 보내주는 것이며, 추후 알림 로직 설계 시
  "데이터가 없는 날은 정상적인 휴장일일 수 있다"는 전제를 반영해야 한다.

**발생한 문제 및 해결**

| 문제 | 원인 | 해결 |
|---|---|---|
| 사이트에서 "Open API" 버튼을 찾지 못함 | ECOS 웹 UI 개편으로 버튼 위치/이름이 예상과 다름 | API 자체(`StatisticTableList`)로 통계표를 검색하는 우회 방법을 고안 — 결과적으로 더 재사용 가능한 방법이었음 |
| `KeyError: 'StatisticTableList'` | `.env`에서 API 키를 못 불러와 `None`이 URL에 그대로 들어감 | `.env` 파일이 실제로 존재하지 않았던 것으로 확인, 파일을 직접 생성해 해결 |
| API 키를 코드에 직접 하드코딩 | `.env` 대신 f-string에 키 값을 그대로 입력 | 변수(`{api_key}`)로 참조하도록 수정 |
| `.env`에서 값을 계속 `None`으로 불러옴 | `.env` 파일이 프로젝트 루트가 아닌 위치에 있었거나 애초에 생성되지 않음 | `ls -la`로 파일 존재 여부를 직접 확인하는 습관 습득 |
| `main.py` 실행 시 작년 날짜 데이터가 출력됨 | 예시 코드에 넣었던 고정 날짜(`20250901`)를 그대로 사용 | `datetime`/`timedelta` 기반 자동 날짜 계산으로 전환 |

---

### 2.3 단계 2 — Git / GitHub 워크플로우 트러블슈팅

**발생한 문제: 브랜치 이름 불일치**

로컬 저장소는 기본적으로 `master`라는 브랜치명으로 생성되어 있었는데,
GitHub 저장소의 기본 브랜치는 `main`이었다. 이로 인해 `git remote add`로
연결은 되어 있었지만, `git log`에는 커밋이 하나도 없다고 뜨고, GitHub에
있던 `README.md`, `.gitignore` 파일이 로컬 폴더에는 전혀 보이지 않는
현상이 발생했다.

**진단 과정 (실제 디버깅 흐름)**
```
git remote -v   → origin 연결은 정상 확인
git log         → "커밋이 없다"는 메시지 확인 (이상 신호)
git status      → 브랜치는 master, 추적 중인 파일 없음
git branch -a   → 아무것도 출력 안 됨 (원격 정보를 가져온 적이 없음)
git fetch origin → 원격 정보 수신
git branch -a   → remotes/origin/main 확인 → 원인 파악
```

**해결**
```
git checkout -b main origin/main
```
로컬에 `main` 브랜치를 새로 만들면서 원격의 내용을 그대로 받아오는 방식으로 해결.

**교훈**: `git init` 후 수동으로 `remote add`를 할 경우 브랜치명 불일치가
발생할 수 있으므로, 가능하면 `git clone`으로 시작하는 편이 이런 문제를
원천적으로 방지한다는 것을 체감했다.

---

### 2.4 단계 3 — 보안 사고: API 키 노출 및 대응

프로젝트 진행 중 가장 중요한 실무적 교훈을 얻은 단계.

**사고 경위**
1. `.env` 파일을 `.gitignore`에 등록하기 전에, 실수로 `.env` 파일을
   `git add` → `git commit` → `git push` 한 이력이 있었음
2. 이후 `.gitignore`에 `.env`를 추가했지만, **Git은 이미 한 번 추적(tracking)을
   시작한 파일에 대해서는 `.gitignore` 규칙을 적용하지 않는다**는 사실을
   모른 채 작업을 계속함
3. `git status` 결과에서 `.env`가 "modified"로 표시되는 것을 발견 →
   `.env`가 여전히 Git의 추적 대상임을 인지
4. `git log --all --full-history -- .env`로 확인한 결과, ECOS API 키가
   담긴 커밋 2건이 이미 GitHub(퍼블릭 저장소)에 푸시되어 있었음을 확인

**대응 조치**
1. ECOS 사이트에서 노출된 API 키를 즉시 재발급 → 기존 유출 키를 무효화
2. `git rm --cached .env` 명령으로 `.env`를 Git 추적에서 제거
   (로컬 파일은 그대로 유지, Git의 관리 대상에서만 제외)
3. 새로 발급받은 키로 `.env` 내용을 갱신
4. 재발급 이전에 DB 비밀번호(`DB_PASSWORD`)는 아직 한 번도 커밋되지
   않았음을 사전에 확인 → 노출 전에 문제를 차단

**핵심 교훈**
- `.gitignore`는 **아직 추적되지 않은 파일**에만 효력이 있다. 이미 추적 중인
  파일은 `git rm --cached`로 명시적으로 추적을 해제해야 한다.
- 새 프로젝트를 시작할 때는 **민감 파일을 만들기 전에 먼저 `.gitignore`부터
  작성**하는 순서가 안전하다.
- 키가 한 번이라도 퍼블릭 저장소에 노출되면, 커밋 히스토리 자체를 지우는 것보다
  **키를 무효화(재발급)하는 것이 가장 확실하고 즉각적인 대응**이라는 것을 배움
  (히스토리에 과거 키가 남아있어도, 그 키가 더 이상 유효하지 않으면 실질적 위험은 사라짐).
- 커밋할 때 `git add .`로 전체를 한 번에 올리지 않고 파일을 지정해서 추가하는
  습관이, 이런 사고를 조기에 발견하는 데 실질적으로 도움이 된다는 것을 체감.

---

### 2.5 단계 4 — 데이터베이스 설계 및 구축: SQLite → Oracle Cloud 전환

**의사결정 과정**

처음에는 SQLite(파일 기반, 설치 불필요)로 시작할 계획이었으나, SQLD 자격증
공부 경험을 살려 DBeaver + Oracle SQL 환경을 그대로 활용하고 싶다는
의견을 반영해 Oracle Cloud로 방향을 틀었다.

**Oracle DB를 어디에 둘지에 대한 트레이드오프 검토**

| 옵션 | 장점 | 단점 |
|---|---|---|
| 로컬 설치 (Oracle XE) | 인터넷 연결 불필요 | 설치 용량이 크고(수 GB), Windows 서비스 관리가 까다로움 |
| **오라클 클라우드 무료 티어 (선택)** | 설치 불필요, 컴퓨터를 꺼도 DB는 유지됨, 이후 상시 구동 서버와 연동하기 용이 | 클라우드 콘솔 조작에 익숙해질 필요, 네트워크 설정 이해 필요 |

**진행 과정에서 겪은 현재성 이슈**
- 가입 시 홈 리전(Home Region) 목록에 "Seoul"이 뜨지 않는 문제 발생
  → 무료 티어(Always Free) 컴퓨팅 용량이 리전별로 제한적이기 때문에 발생하는
  잘 알려진 현상. 지리적으로 가까운 **Japan East (Tokyo)** 리전으로 대체 선택.
- 서비스명이 "Autonomous Database"가 아니라 **"Autonomous AI Database"**로
  변경되어 있었음 (오라클의 최근 리브랜딩). 기능상 동일한 서비스임을 확인 후 진행.

**Display Name vs Database Name 개념 정리**
- **Display Name**: 콘솔에 보이는 사람이 읽기 위한 별명. 자유롭게 수정 가능.
- **Database Name**: 실제 접속 시 사용되는 고유 식별자. 영문·숫자만 가능하며
  생성 후 변경이 어려움.

**DB Wallet 개념**

Oracle Autonomous Database는 접속 시 mTLS(상호 인증) 기반 보안을 기본으로
하며, 이를 위해 "Wallet"이라는 인증서·설정 파일 묶음을 다운로드해야 한다.
로컬의 DBeaver, 파이썬 드라이버 모두 이 wallet 폴더 경로를 알아야 DB에
접속할 수 있다.

**발생한 문제 및 해결**

| 문제 | 원인 | 해결 |
|---|---|---|
| `Database connection is not available on a database in provisioning state.` | DB가 아직 완전히 프로비저닝되지 않음 | 상태가 "Available"로 바뀔 때까지 대기 |
| `ORA-12506: TNS: 리스너가 서비스 ACL 필터링에 따라 접속을 거부했습니다` (DBeaver) | Access type/ACL 설정은 정상("everywhere", ACL 비활성화)이었음에도 계속 발생 — 원인은 DBeaver에 내장된 Oracle JDBC 드라이버 버전이 최신 wallet 방식 인증을 온전히 지원하지 못했을 가능성이 유력 | 드라이버 갱신 시도로도 미해결 → DBeaver 접속 디버깅은 잠정 보류하고, 파이썬 `oracledb` 드라이버로 우회하여 프로젝트 진행 재개 (도구가 목적이 아니라 수단이라는 점을 인지하고 실용적으로 판단) |
| `DPY-4027: no configuration directory specified` (파이썬) | `.env`에 `WALLET_DIR` 값이 누락되어 `config_dir`가 빈 값으로 전달됨 | `.env`에 wallet 폴더의 절대경로를 추가 |
| `Enter PEM pass phrase:` 무한 대기 | Thin 모드 접속 시, wallet zip 다운로드할 때 설정한 **wallet 자체의 비밀번호**(DB 계정 비밀번호와는 별개)를 코드에서 전달하지 않음 | `oracledb.connect()`에 `wallet_password` 파라미터 추가 |
| `ORA-00942: table or view "ADMIN"."RATES" does not exist` | 테이블 생성 스크립트(`db.py`)를 실행하기 전에 데이터 저장 스크립트(`main.py`)부터 실행함 | 실행 순서를 `db.py` → `main.py`로 정정 |

**Oracle SQL 문법 관련 학습**
- Oracle은 SQLite와 달리 `CREATE TABLE IF NOT EXISTS` 구문이 버전에 따라
  다르게 동작하므로, "생성을 시도하고, 이미 존재한다는 에러(ORA-00955)가
  나면 무시하는" 방어적 코드 패턴을 사용함
- 컬럼 타입 매핑: SQLite `TEXT`/`REAL` → Oracle `VARCHAR2`/`NUMBER`
- 중복 데이터 방지를 위해 `UNIQUE(rate_date, currency)` 제약조건을 설계
  → 같은 통화의 같은 날짜 데이터가 중복 삽입될 경우 `IntegrityError`를
  발생시키고, 이를 `try/except`로 잡아 "이미 존재함"으로 처리

---

## 3. 전체 아키텍처 현재 상태

```
find_code.py   : ECOS 전체 통계표 목록에서 필요한 표의 코드를 검색하는 유틸리티
find_item.py   : 특정 통계표 안의 세부 항목 코드를 검색하는 유틸리티
db.py          : Oracle DB 연결 함수(get_connection) + 테이블 생성(init_db)
main.py        : ECOS API 호출 → 날짜별 환율 데이터 수신 → Oracle DB에 저장
check_db.py    : DB에 저장된 데이터를 조회해 확인하는 검증용 스크립트
.env           : API 키, DB 비밀번호, wallet 경로 등 민감 정보 (Git 추적 제외)
.gitignore     : venv/, .env, wallet 폴더 등을 Git 추적에서 제외
requirements.txt : 재현 가능한 환경을 위한 의존성 목록
```

**데이터베이스 스키마 (rates 테이블)**

| 컬럼명 | 타입 | 설명 |
|---|---|---|
| id | NUMBER (AUTO INCREMENT) | 기본키 |
| rate_date | VARCHAR2(8) | 날짜 (YYYYMMDD) |
| currency | VARCHAR2(20) | 통화 코드 (예: USD) |
| value | NUMBER | 환율 값 |
| (제약조건) | UNIQUE(rate_date, currency) | 날짜+통화 조합 중복 방지 |

---

## 4. 종합적으로 얻은 실무 역량

1. **REST API를 다루는 일반적인 탐색 전략**
   (목록 조회 → 항목 특정 → 실제 데이터 요청)을 실전에서 체득
2. **환경변수 기반 민감정보 관리**와, 그 원칙이 한순간의 실수로 무너질 수
   있다는 것, 그리고 사고가 발생했을 때 **당황하지 않고 원인을 역추적해
   대응하는 절차**를 실제로 경험
3. **Git 브랜치 개념의 실질적 이해** — 단순 암기가 아니라, 브랜치가 어긋났을
   때 어떤 증상이 나타나는지, 어떤 명령어 조합으로 진단하는지를 실전에서 습득
4. **클라우드 데이터베이스의 인증 구조(mTLS, Wallet)**에 대한 이해 —
   단순 ID/PW 인증이 아닌, 인증서 기반 보안이 실무에서 어떻게 쓰이는지 체험
5. **막히는 도구에 매몰되지 않고 대안 경로로 전환하는 실용적 판단력**
   (DBeaver 연결 이슈를 잠정 보류하고 파이썬 드라이버로 먼저 목표를 달성한 것)

---

## 5. 다음 단계 (TODO)

- [ ] 스케줄러(APScheduler 등)를 이용한 주기적 자동 데이터 수집
- [ ] 뉴스/정세 데이터 소스(네이버 뉴스 API 또는 GDELT) 연동
- [ ] 변동폭 감지 로직 및 AI 기반 설명 생성 로직 구현
- [ ] 텔레그램 봇을 통한 실시간 알림 기능
- [ ] Flask 기반 대시보드로 현재 상태 시각화
- [ ] (선택) DBeaver 접속 이슈 재도전 — 프로젝트가 안정화된 후 재조사
